# OpenRouter API Key (required by the "openrouter_free" profile)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Optional: override which profile in config/llm_profiles.yaml is active
# (defaults to that file's `active_profile`). e.g. LLM_PROFILE=local_ollama
# LLM_PROFILE=openrouter_free